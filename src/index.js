import React from 'react';
import ReactDOM from 'react-dom';
import { ManduApp } from './ManduApp';

ReactDOM.render(
  <React.StrictMode>
    <ManduApp />
  </React.StrictMode>,
  document.getElementById('root')
);
